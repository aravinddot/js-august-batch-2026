

// const name = "playwright"

// const upperCaseName = "PLAYWRIGHT"

// // console.log(name.toUpperCase()) // convert the string to uppercase
// // console.log(upperCaseName.toLowerCase()) // convert the string to lowercase

// // console.log(name.includes("play")) // check if the string includes the given substring

// console.log(name.indexOf('y')) // check the index of the given substring in the string

// console.log(name.slice(0, 4)) // extract a part of the string from the given index


// const tool = "Playwright Automation Playwright Automation"

// console.log(tool.replace("Playwright", "Selenium")) // replace the given substring with the new substring

// console.log(tool.replaceAll("Playwright", "Selenium")) // replace all the given substring with the new substring


// const tool = " automation "

// console.log(tool.trim()) // remove the whitespace from the start and end of the string


// const tool = "playwright"

// const name = "automation"

// // console.log(tool.concat(" ", name)) // concatenate two strings

// // console.log(tool + " " + name) // concatenate two strings

// console.log(`${tool} ${name}`) // concatenate two strings




// const lang = "JavaScript"

// // console.log(lang.startsWith("Java")) // check if the string starts with the given substring

// // console.log(lang.endsWith("Script")) // check if the string ends with the given substring

// console.log(lang.split('').reverse().join(''))